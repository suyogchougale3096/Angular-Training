import { SpecialcharremovePipe } from './specialcharremove.pipe';

describe('SpecialcharremovePipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialcharremovePipe();
    expect(pipe).toBeTruthy();
  });
});
